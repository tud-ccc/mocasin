defaults:
  - common
  - hydra/job_logging: pykpn
  - slx: default
  - tgff: default
  - kpn: default
  - platform: default
  - mapper: from_file
  - source: slx

output_file: "${kpn.name}_on_${platform.name}.dot"
