platform:
  name: test
  file: ???
  class: pykpn.slx.platform.SlxPlatform
  params:
    name: ${platform.name}
    platform_xml: ${platform.file}
    slx_version: ${slx.version}

kpn:
  name: ???
  file: ???
  class: pykpn.slx.kpn.SlxKpnGraph
  params:
    name: ${kpn.name}
    cpngraph: ${kpn.file}
    slx_version: ${slx.version}

mapping:
  name: ${kpn.name}_on_${platform.name}
  file: ???
  class: pykpn.slx.mapping.slx_read_fullmapper.slx_read_mapper
  mapping_xml: ${mapping.file}
  slx_version: ${slx.version}

trace:
  directory: ???
  class: pykpn.slx.trace.SlxTraceReader
  params:
    trace_dir: ${trace.directory}
    prefix: ${kpn.name}.
    slx_version: ${slx.version}
