# @package _global_
defaults:
  - common
  - platform: default
  - resource_manager: default
  - override hydra/job_logging: mocasin
  - _self_

# Base directory with applications and mappings info
tetris_apps_dir: ???

# Scenario to schedule
input_jobs: ???

# Output trace
output_trace:

# Output file for summary
summary_csv:

# Allow migration of the tasks
allow_migration: true
