# @package _global_
defaults:
  - common
  - platform: default
  - resource_manager: default
  - override hydra/job_logging: mocasin
  - _self_

# Base directory with applications and mappings info
tetris_apps_dir: ???

# Scenario to schedule
input_jobs: ???

# Output trace
output_trace:

# Output statistics and summary results
stats_jobs: "stats_jobs.csv"
stats_manager: "stats_manager.csv"
summary: "summary.csv"
