# @package _global_
defaults:
  - common
  - hydra/job_logging: mocasin
  - tgff: default
  - graph: default
  - platform: default
  - trace: default
  - representation: SimpleVector
  - mapper: default
  - sdf3: default

output_file: "${graph.name}_on_${platform.name}.dot"
