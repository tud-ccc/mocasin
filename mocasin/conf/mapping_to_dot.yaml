# @package _global_
defaults:
  - common
  - hydra/job_logging: mocasin
  - tgff: default
  - kpn: default
  - platform: default
  - trace: default
  - representation: SimpleVector
  - mapper: default
  - sdf3: default

output_file: "${kpn.name}_on_${platform.name}.dot"
