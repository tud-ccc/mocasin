mapping:
  name: ${kpn.name}_on_${platform.name}
  file: "../../../slx/app/${kpn.name}/${platform.name}/default.mapping"
  class: pykpn.mapper.slx_read_fullmapper.slx_read_mapper
  mapping_xml: ${mapping.file}
  slx_version: ${slx.version}
