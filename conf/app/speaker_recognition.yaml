app_name:
  speaker_recogintion
cpn_xml:
  ../../../apps/${app_name}/${app_name}.cpn.xml
start_time:
  0 us
mapping_xml:
  ../../../apps/${app_name}/${platform_name}/default.mapping
trace_dir:
  ../../../apps/${app_name}/${platform_name}/traces
